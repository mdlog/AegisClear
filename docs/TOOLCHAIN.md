# Toolchain (diverifikasi 2026-09-19)
- forge/cast/anvil: forge Version: 1.5.1-stable
- node: v22.23.1, pnpm: 9.15.0
- circom: circom compiler 2.2.3
- python3: Python 3.12.3
- cargo: cargo 1.92.0 (344c4567c 2025-10-21), cargo-stylus: stylus 0.10.9
- rustup targets: wasm32-unknown-unknown wasm32v1-none x86_64-unknown-linux-gnu 
- web console (Task 7, 2026-09-20; `pnpm --filter @aegisclear/web list --depth 0`): vite 6.4.3, react 18.3.1, @vitejs/plugin-react 4.7.0, vitest 5.0.1

## sla_settlement.circom (Task 4, 2026-09-19)
- `SlaSettlement(128, 7)` (MAX_SEQ=128, DEPTH=7 — unchanged, D4 not applied), compiled with `circom --O2` (full constraint simplification; see `circuits/scripts/build.sh`).
- Note: circom's CLI default (`--O1`, no flag) yields ~218k constraints (above the gate) on this same circuit. `--O2` is a lossless, semantics-preserving substitution of the linear rows, not a circuit redesign. See `task-4-report.md` for why D4 (N=64/DEPTH=6) was evaluated and rejected instead (breaks the 100/100/128-receipt vectors).
- `pnpm --filter @aegisclear/circuits build` wall time: ~9.7–9.8s.

### Fix round 1 (2026-09-19) — superseded the numbers above
Review found `DivBps`'s remainder `r` was range-checked only via `LessThan(14)` (unsound for out-of-range `in[0]`, per circomlib's own precondition), and `seq`/`penaltyBps`/`capBps` were only bounded by `LessEqThan`, which is likewise unsound without a prior bit-decomposition. Fixed by adding `Num2Bits(14)` on `r` (in `DivBps`, before its `LessThan(14)`), `Num2Bits(8)` on `seq`, and `Num2Bits(14)` on `penaltyBps`/`capBps` (both before their existing `LessEqThan(14)` checks). See `task-4-report.md` § "Fix round 1" for the full finding text and regression-test evidence.
- **`snarkjs r1cs info` (post-fix-round-1)**: # of Wires: 113910, # of Constraints: 115066 (< 131072 gate), # of Private Inputs: 518, # of Public Inputs: 6, # of Labels: 319867, # of Outputs: 0. (+1842 constraints vs. the pre-fix 113224, from the 4 new `Num2Bits` range checks × 128 `DivBps` instances + globals.)

### Fix round 2 (2026-09-19) — DivBps split into a shared constraint template; regression test rewritten
Fix round 1's regression test (tampering `main.div[0].q`/`.r` directly in a full `sla_settlement` witness after `calculateWitness`) was found NON-DISCRIMINATING: it also failed with `rb` removed, because the tampered `q`/`r` left the *other* pre-existing bit-decomposition sub-signals (`qb.out[]`, `rlt`'s internal `n2b.out[]`) stale/inconsistent with the new values, so `checkConstraints` rejected for an unrelated reason regardless of whether `rb` was present. Fixed by extracting `DivBps`'s constraint set into `template DivBpsConstraints()` (`circuits/lib/divbps.circom`, taking `x,q,r` as circuit **inputs**, not witness-derived signals) — `sla_settlement.circom` now `include`s it and its own `DivBps` wraps it; a standalone probe circuit (`circuits/test/circuits/divbps_probe.circom`, `component main = DivBpsConstraints();`) lets the test drive `(x,q,r)` directly through `calculateWitness`, so every dependent bit signal is freshly and consistently computed for the attacker-chosen values — no stale bits, no cross-contamination with unrelated constraints. See `task-4-report.md` § "Fix round 2" for the full discrimination proof (test run with `rb` temporarily removed → FAILS; restored → PASSES).
- **`snarkjs r1cs info` (current, post-fix-round-2, same circuit semantics as fix round 1, just restructured)**: # of Wires: 113910, **# of Constraints: 115066** (< 131072 gate, unchanged from fix round 1 — the refactor is constraint-count-neutral), # of Private Inputs: 518, # of Public Inputs: 6, # of Labels: 320254, # of Outputs: 0.

## Trusted setup, verifier, prove CLI (Task 5, 2026-09-19)
- Both public Hermez ptau mirrors (`storage.googleapis.com/zkevm/ptau`, `hermez.s3-eu-west-1.amazonaws.com`) returned HTTP 403 (verified 19 Sep 2026) → `circuits/scripts/setup.sh` generates Powers of Tau 2^17 locally (single contributor + verifiable beacon: `new`/`contribute`/`beacon`/`prepare phase2`/`verify`). Local ptau pipeline: **~5m31s** (12:54:18–12:59:49); `powersoftau verify` on the final ptau — OK.
- Groth16 phase 2 on `sla_settlement.r1cs` (115,066 constraints): `groth16 setup` ~53s, `zkey contribute` + `zkey beacon` (single contributor) a few seconds, `zkey verify build/sla_settlement.r1cs ptau/pot17_final.ptau build/sla_final.zkey` → **ZKey Ok!**. Total `setup.sh` wall time: **447s (~7m27s)**, rc=0.
- Artifact sizes: `circuits/build/sla_final.zkey` = 100,834,787 bytes (~96.2 MiB; gitignored, not committed, present on disk); `circuits/build/verification_key.json` = 3,842 bytes (committed); `contracts/src/SLASettlementVerifier.sol` = **203 lines** (snarkjs-exported Groth16 verifier, contract renamed to `SLASettlementVerifier`; `function verifyProof(uint[2] calldata _pA, uint[2][2] calldata _pB, uint[2] calldata _pC, uint[6] calldata _pubSignals) public view returns (bool)`, 6 public inputs).
- `circuits/scripts/prove.ts` CLI (`groth16.fullProve`) for vector `EX1_7_latency_breaches`: **provingMs ≈ 4.1–4.7s** (measured 4132 ms and 4679 ms across separate runs) — well under the 30s D4 threshold (§ V11), no circuit-size mitigation needed. Independently cross-checked: `snarkjs groth16 fullprove` + `snarkjs groth16 verify build/verification_key.json public.json proof.json` on the same vector → **OK**.
- `contracts/test/fixtures/ex1_verifier.json`: EX1 proof+inputs snapshot for verifier gas tests; `inputs[0]="511"` (channel `0x1ff`), `inputs[3]="100"` (seq), `inputs[4]="2000000"` (cumulativeAmount), `inputs[5]="70000"` (payToClient).
- Full `circuits` suite incl. new `test/prove.test.ts` (14 tests): PASS, ~11.9s wall. TDD RED captured first: `Error [ERR_MODULE_NOT_FOUND]: Cannot find module '.../circuits/scripts/prove.ts'` (12 pass / 2 fail) before `scripts/prove.ts` was written.

## Foundry scaffold, MockUSDG, verifier gas (Task 6, 2026-09-19)
- `contracts/` scaffolded via `forge init --no-git --force .` (forge 1.5.1-stable); Counter* boilerplate removed. `forge install`'s `--no-commit` flag no longer exists on this forge version (replaced by opt-in `--commit`), so plain `forge install <org>/<repo>[@tag]` was used instead — behaviorally equivalent here since the git root is `AegisClear/`, not `contracts/`.
- `forge install foundry-rs/forge-std` failed (`'contracts/lib/forge-std' already exists and is not a valid git repo`, since `forge init --no-git` had already populated it as a plain, non-submodule directory) → used the brief's fallback: removed the plain directory, then `git submodule add https://github.com/foundry-rs/forge-std contracts/lib/forge-std` from the repo root. `openzeppelin-contracts@v5.7.0` and `permit2` installed cleanly via `forge install` and self-registered in the root `.gitmodules` at `contracts/lib/...`.
- Submodule commits: `forge-std` @ `3e577a1` (default branch tip), `openzeppelin-contracts` @ `cab1993` (tag `v5.7.0`, as pinned), `permit2` @ `cc56ad0` (default branch tip; brief did not pin a tag).
- Removed `forge init`'s auto-generated `contracts/README.md` and `contracts/foundry.lock` (neither is in the brief's file list; `foundry.lock` was also stale — it recorded the pre-fallback `forge-std` rev and never picked up `permit2`).
- Canonical fixture bytecode from Robinhood Chain mainnet (`https://rpc.mainnet.chain.robinhood.com`, both `cast code` calls succeeded first try, no retry needed): `contracts/test/fixtures/permit2.bytecode` = 18,306 bytes, `contracts/test/fixtures/x402proxy.bytecode` = 5,828 bytes (both > 1000 bytes per the gate; no trailing newline, verified with `tail -c1 | xxd`).
- `forge test --match-contract VerifierTest -vv`: 3/3 PASS. **`verifyProof gas (6 public inputs): 229241`** (EX1 fixture, < 300,000 gate) — copied to spec §8.7 `claimPenalty` row. Tampered-public-input and tampered-proof cases both return `false` (not revert); the tampered-proof case reports very high per-test gas (~1.04B) because the flipped bit produces an invalid G1 point and the `ecPairing` precompile (address `8`) consumes all gas forwarded to it on invalid input — expected EVM precompile behavior, not a bug, and unrelated to the `assertLt` gate (which only applies to the valid-proof case).
- `forge build` (forced clean rebuild): compiles successfully but **not** warning-free — 2× solc `Warning (2018): Function state mutability can be restricted to view`, both on `Verifier.t.sol`'s `test_tampered_public_input_fails()`/`test_tampered_proof_fails()`, which the task brief specifies verbatim as `public` (not `view`); left as-is rather than deviating from the brief's exact code. Separately, forge's built-in linter (`forge-lint`, note-severity, distinct from solc warnings) flagged `screaming-snake-case-const` throughout the pre-existing (Task 5) `SLASettlementVerifier.sol` and `unsafe-cheatcode` for `vm.readFile` in the test — both are pre-existing/brief-mandated code, not modified.

## ERC-1271 client, invariant handler, settlement fuzz, event assertions (Task 12, 2026-09-19)
- `forge test --match-contract "Wallet1271Test|InvariantTest|SettleFuzzTest|EventsTest"`: **13/13 PASS** — 2 ERC-1271 (`Wallet1271Test`), 4 invariants (`InvariantTest`, configured `runs=64 depth=32` → 2048 calls/invariant, 0 reverts across all 5 handler selectors), 1 fuzz (`SettleFuzzTest`, `runs=256`), 6 event-assertion tests (`EventsTest`, controller-mandated carry-over).
- Whole suite: `forge test` → **57/57 PASS** (41 pre-existing + 16 new: 2 `Wallet1271Test` + 4 `InvariantTest` + 1 `SettleFuzzTest` + 6 `EventsTest` [controller-mandated] + 1 `Penalty.t.sol` stale-proof-replay test [controller-mandated] + 2 optional boundary tests [`seq_==128` in `Checkpoint.t.sol`, `toProvider==budget()` in `Cooperative.t.sol`]).
- `forge coverage --report summary`: **`src/AegisChannel.sol` = 100.00% line (95/95)** (≥ 90% gate met), 94.59% statements (140/148), 77.42% branches (24/31), 100% functions (16/16). Two pre-existing branch gaps noted but not fixed (out of Task 12's "no contract source changes" scope; both are in code shipped by earlier tasks): `closeCooperative`'s own `seq_ > MAX_SEQ` check (line 191 — only `submitCheckpoint`'s equivalent `SeqTooLarge` path is exercised) and `_requireBothSigned`'s client-side signature check (line 229 — every existing co-signed-op test that reverts `BadSignature` supplies an invalid *provider* signature with a valid client signature; none exercises the reverse).
- Real-proof FFI test (`Penalty.t.sol`, `npx tsx ../circuits/scripts/prove.ts`) adds ~5–7s to the full-suite run time (unchanged behavior from Task 11; new `test_stale_proof_rejected_after_newer_checkpoint` reuses the same proof fixture, no extra `vm.ffi` call).
