# SDD ledger — plan: docs/superpowers/plans/2026-09-20-aegisclear-ship.md
Spec: docs/superpowers/specs/2026-09-20-aegisclear-p1-design.md §C. Branch: feat/aegisclear-p1-ship (from main 4144d3d).
Test infra: private chain `~/.local/bin/aegis-node --port 8547` + DeployLocal; web tests :4042; SDK ports 4020–4030/4041/4044.

## Pre-flight conflict scan (20 Sep 2026)
| Pair / task | Produces vs consumes | Finding |
|---|---|---|
| T1 → T2 | `runRollover`, `RolloverResult`, `toRows({bAnch,bRoll})`, `privateValues(terms,{anchored})`, `ANCHORED_UNITS` vs web runner | consistent |
| T2 anchored leak-check | `leakCheck(pc, factory, …)` needs the anchored factory for the ChannelOpened lookup | plan says pass the matching factory — noted |
| T2 numbers | anchored 20 units, breaches 3 & 17 → payToClient 20 000, A 400 000 → `0.02 / 0.38`; rollover 133 units → provider 2.66 | consistent with demo metrics (BREACHES set) and TERMS_BASE |
| T2 settle race | anchored dispute on the console: the anchored provider's watcher may settle first — `runMarketB` already tolerates (Plan A fix) | ok |
| T3 Slither → T6 | contract fixes trigger a testnet v2 redeploy (controller) | accepted |
| T4 spec numbers | only README/ledger numbers | rule stated in plan |
| Rubric | no asserting-nothing tests | clean |

## Task log
- Task 1: dispatched (sonnet). Task 3 (Slither) dispatched in parallel (opus) — disjoint paths (demo/ vs contracts/, docs/audit).
- Task 1: implementer DONE — commit 13c0a9b (demo 5/5). Note for Task 2: anchored ack txs should be flushed per unit for the live log. Review dispatched (sonnet).
- Task 1: complete — commit 13c0a9b (review: Approved). Minor: RolloverResult.local covers only epoch-1 receipts (comment it in Task 2); boilerplate duplication accepted.
- Task 2: dispatched (sonnet) while Task 3 (Slither, opus) still runs — disjoint paths (web/, demo/ vs contracts/, docs/audit/).
- Task 2: implementer DONE — commit 127f92f (web 33/33, demo 5). Review dispatched (sonnet) + controller browser check.
- Task 2: controller API check on :4044 — B-anchored-dispute → row 0.02 / 0.38 (gas 6,766,593 incl. 20 acks, proving 4,315 ms), 20 ack steps, leak-check (0,0,25 tx), channel mode anchored seq 20 SETTLED; B-rollover → 0.00 / 2.66, txs fund/rollover/closeCooperative, channel epoch 1 seq 5 SETTLED. Browser extension was disconnected — visual check deferred to Task 6.
- Task 3: implementer DONE — commits 5fe05c0 + 12841d3 (Slither 77→75; 2 Low fixed; manual M-1 hook gas starvation (63/64) fixed with InsufficientGas guard; forge 118). Review dispatched (opus).
- Task 2: complete — commit 127f92f (review: Approved; minors: report wording, no /provider-anchored 404 test).
- Task 4: dispatched (opus) — spec v1.1 (prd-arsitektur.md only).
- Task 5: dispatched (submission-packager agent) → docs/SUBMISSION.md, controller commits after fact-check. Running in parallel with Task 4 (spec) and the Task 3 review.
- Task 3: complete — commits 5fe05c0 + 12841d3 (+ controller precision notes) (review opus: M-1 math verified, tests discriminate (3/5 fail pre-fix), triage tallies reproduced; Approved).
- Task 5: complete — docs/SUBMISSION.md (packager agent; all 17 addresses trace to repo sources; HackQuest page fetched: criteria = Smart contract quality · Product-Market Fit · Innovation & Creativity · Real Problem Solving (+USDG consideration, 1/3 prizes Robinhood Chain); registration closes 2026-10-02T17:01Z; submission 2026-10-04T15:59Z; T&C PDF blocked (manual read needed)). Action items carried: v2 redeploy (Task 6), README inconsistency about the committed deployment JSON (Task 6 docs pass), console run on testnet (Task 6), V8 facilitator test + mainnet (user decisions).
- Task 6 (controller): testnet v2 deployed (commit a5ee8ba; deployBlock 122028843; POSEIDON_STYLUS 0x1027cf7D…ef34 known-answer OK). SDK testnet suite v2: 14 passed / 1 failed / 4 skipped (973 s); the failure was a stale `balanceOf` read from the public RPC load balancer right after the receipt (on-chain Settled 190000/810000, tx 0x2c39b1…63d6) → `expectEventually` polling (commit f75f4f0); single-test re-run: 1 passed (194 s; ack first 352,421, startClose 64,045, claimPenalty 307,625). Next: v2 docs pass, console-on-testnet API check, full suites, final review.
- Task 6 (controller): v2 docs pass committed (e3e4d6e). Console on testnet v2 via API (:4044, `AEGIS_NETWORK=testnet`; browser extension not connected → API-level only): B-dispute 0.07/1.93 (575,544 gas, 4,165 ms, 158 s, ch 0x4B6F3c6d…bBd1, leak 0/5 tx) · B-anchored-dispute 0.02/0.38 (4,799,914 gas with Stylus vs 6,766,593 Anvil/Yul, 3,404 ms, 140 s, ch 0xAD30BC16…879c, leak 0/25 tx) · B-rollover 0.00/2.66 (298,821; rollover 137,539; ch 0xD1F69213…2b1D epoch 1 seq 5) · B-cooperative 0.00/2.00 (182,452, 96 s) · A-complete 0/2.00 (384,176) · A-reject 2.00/0 (367,017); `open` 308,476–312,398. Docs updated (README browser table, spec §8.7/§14, SUBMISSION runbook Verified column + items 3–5) — commit follows. Observation: `/api/offer` shows the previous SETTLED session's 402 until the next run resets it — documented as a facet of the known "sessions never evicted" limitation (README).
- Task 6 (controller): console-on-testnet docs committed (20d7a01). Full local suites on branch head 20d7a01: forge 118/118, circuits 14, demo 5, stylus 9 (4+1+4), sdk 50/50 (private chain :8547), web 33/33 (needs `RPC_URL=http://127.0.0.1:8547` — a first run without it failed 13 tests on ECONNREFUSED :8545, environment not code), web typecheck clean. Final whole-branch review dispatched (opus) → `.superpowers/sdd/2026-09-20-aegisclear-ship/final-review.md`.
- Final whole-branch review (opus, `final-review.md`): With fixes — 0 Critical, 2 Important (I1 SUBMISSION still said deploy v1 / 13 tests / v1 ack gas / "v2 redeploy" as roadmap; I2 runtime read-after-write unpolled on the LB RPC: client `rollover()` 3×500 ms confirm loop, demo result deltas single read), 8 Minor. Rulings: I1 + M1 (v1 channel labelled history), M2 (ledger link → tracked archive), M5 (spec §16 V18c, two Anvil gas figures explained, README v0 prose), M6 audit sentence, M8 (v1 JSON committed as history) done by controller → commit a52a5a9; I2 + M3 (anchored privacy card) + M7 (comment) + M6 test comment → ONE fix dispatch (sonnet, `final-fix-brief.md`). Parked: M4 (two watchers + provider share one key; nonce collisions retried next tick — harmless; consider a shared nonce manager post-merge); M6 constant duplication stays (exposing HOOK_GAS would change bytecode vs verified v2). Recommendation kept: `_send` guard unchanged.
- Final fix → commits d607bc5 (sdk `rollover()` 15 s/750 ms confirm budget; demo `balanceEventually` for the four result deltas; comment) + 1a5a61a (PrivacyCards anchored `harga/unit` implied on-chain + breach list bounded by anchored seq; HookGas.t.sol constant comment). Implementer suites: forge 118, sdk 50, demo 5, web 33, typechecks clean. Scoped re-review dispatched (sonnet) over 20d7a01..1a5a61a.
- Final re-review (sonnet): all findings addressed except one residual of I1 — `docs/SUBMISSION.md:152` still said "17 integration / 13 pass" → fixed by controller (47c65ed: 19 / 15 on v2). No new issues from the fix; re-reviewer confirmed the 15 s rollover budget cannot hit vitest's 120 s testTimeout and that nothing globs `contracts/deployments/*.json`. Full suites re-run on tip 47c65ed before merge (log: scratchpad/suites-final.log).
- Parked (post-merge candidates): M4 watcher/provider shared-key nonce collisions (retried next tick); D11 co-signed `startClose()`; T15 keepalive watcher; no recovery script for an OPEN channel left by a failed rollover run (runbook §2.3 documents the manual recipe); browser rendering of the console on testnet not eyeballed (API-verified only).
- Full suites on tip 47c65ed: forge 118, circuits 14, demo 5, stylus 9, sdk 50, web 33, typechecks clean — green. Ledger archived to docs/superpowers/ledger-2026-09-20-aegisclear-ship.md; branch ff-merged into main and pushed (user pre-approved merge+push after green, 20 Sep 2026). Plan C (ship) complete. Remaining decisions for the owner: mainnet 4663 deploy (needs bridged ETH + USDG), Mesh facilitator V8 test, video recording, HackQuest registration (closes 2026-10-02 17:01 UTC) and submission (closes 2026-10-04 15:59 UTC), T&C PDF manual read, team/contact fields.
