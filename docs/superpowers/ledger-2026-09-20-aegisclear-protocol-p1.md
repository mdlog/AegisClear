# SDD ledger — plan: docs/superpowers/plans/2026-09-20-aegisclear-protocol-p1.md
Spec: docs/superpowers/specs/2026-09-20-aegisclear-p1-design.md §B. Branch: feat/aegisclear-p1-protocol (from main 6f9fce3, web console merged).
Test infra: private Anvil :8547 (foreign Anvil may sit on :8545); DeployLocal → contracts/deployments/local.json (re-broadcast after every contract change); SDK tests 4020/4023/4024/4026/4041, new 4027/4028; web tests 4042.

## Pre-flight conflict scan (20 Sep 2026)
| Pair / task | Produces vs consumes | Finding |
|---|---|---|
| T1 → T2 | typehash strings Checkpoint/Close/Rollover, `hashCheckpoint(epoch,seq,amount,root)` arg order, `epoch()`, `rollover(uint64,uint128,bytes,bytes)`, `RolledOver` event vs SDK types/ABI/tx helpers | identical strings and orders |
| T1 → T5 | `_resetEpochState()` internal virtual (empty) vs T5 body `if (ANCHORED) delete filledSubtrees;` | T5 drops virtual/override; consistent |
| T1/T3 | rollover payout `safeTransfer` (T1) → `_send` (T3) | T3 names the line |
| T2 ↔ merged web console | sign-first `/close` (client signs, provider countersigns) and `session.closing` vs web runner `resetSession`/`runMarketB` (black-box `AegisClient`) | no conflict; web suite must be re-run after T2 |
| T3 mocks | `FreezableToken._update(from,to,value)` override vs OZ v5.7 ERC20 | API exists |
| T4 → T5/T6 | fixture keys zeros/leaves/cumulative/roots/termsCommitment/emptyRoot (decimal strings; forge `readUintArray` coerces numeric strings) vs consumers | consistent |
| T4/T6 | `IPoseidonPath.insertPath(uint256,uint256,uint256[7]) → (uint256,uint256[7])` vs Stylus `insert_path` return `(U256,[U256;7])` | consistent (fallback documented if the SDK macro rejects it) |
| T5 → T7 | `ack(uint64,bytes32,uint128,bytes)`, `hashLeaf(epoch,seq,leaf,amount)`, `Leaf(...)` typehash string vs SDK `LEAF_TYPES`/`ackTx` | identical |
| T5 DeployLocal → T7 test | `factoryAnchored` key in local.json | consistent |
| Global | SDK + web suites red between T1 and T2 (typed data) — documented in plan constraints; DeployLocal must be re-broadcast after T1, T3, T5 | accepted |
| Rubric | no asserting-nothing tests; TDD steps present in every task | clean |

## Task log
- Task 1: dispatched (implementer sonnet), BASE 6f9fce3, brief task-1-brief.md, report task-1-report.md.
- Task 1: implementer DONE — commit 299d5c6 (forge 74/74; AegisChannel runtime 10,190 B). Review dispatched (sonnet).
- Task 1: complete — commit 299d5c6 (review sonnet: ✅ spec, Approved, 0 Critical/Important). Minors: invariant fuzz has no rollover action → folded into Task 8 Step 3b; PaymentReleased emitted at toProvider=0 (consistent with _payout); StaleCheckpoint error name reused (plan-mandated).
- Task 2: dispatched (implementer sonnet), brief task-2-brief.md, report task-2-report.md.
- Task 2: implementer DONE — commit 09262e9 (sdk 36/36, forge 74, web 31). Review dispatched (sonnet).
- Task 2: review (sonnet) → Needs fixes: Important #1 client bumps epoch before ticket check (plan-mandated), #2 /rollover/confirm non-idempotent (retry/spoof strands the exit ticket; plan-mandated). Ruling: verify-then-commit ordering; confirm idempotent by chain state (replay when epoch already current, reset only on epoch+1/seq 0), client retries confirm ×3; adversarial /rollover tests. Fix round 1 → resumed implementer a5c519f. Minor parked: watcher test for epoch mismatch (needs rollover fixture).
- Task 2: fix round 1 → commit a10705a (sdk 36/36, web 31/31, forge 74). Scoped re-review dispatched (sonnet).
- Task 2: complete — commits 09262e9 + a10705a (re-review: all findings addressed). Parked: no test for confirm while closing && epoch not advanced (coded correctly); design note for spec §20 — after a rollover a malicious provider withholding the epoch-1 exit ticket locks the client's remaining budget (co-signed mode has no client-only exit); candidate remedy: allow `startClose()` in co-signed mode too (unilateral seq-0 exit, safe under the responder assumption, would make the exit ticket redundant).
- Task 3: dispatched (implementer sonnet), brief task-3-brief.md, report task-3-report.md.
- Task 3: implementer DONE — commit b480eb1 (forge 83, sdk 37, web 31; settle w/ gas-burner payee 212,572). Review dispatched (sonnet).
- Task 3: complete — commit b480eb1 (review sonnet: ✅ spec, Approved, 0 Critical/Important). Minors folded into Task 8 Step 3c (router nonReentrant, malformed return data, forwarded=false event assertion); routerAbi omissions parked.
- Task 4: dispatched (implementer sonnet), brief task-4-brief.md, report task-4-report.md.
- Task 4: implementer DONE_WITH_CONCERNS (gas 222,766 vs expected ≈225–240k — fine) — commit c6c1059 (forge 88, circuits 14). Review dispatched (sonnet).
- Task 4: complete — commit c6c1059 (review sonnet: ✅ spec, Approved, 0 findings of note). Controller cross-checked roots[1], roots[2], termsCommitment against the pre-computed constants: match.
- Task 5: dispatched (implementer sonnet), brief task-5-brief.md, report task-5-report.md.
- Task 5: implementer DONE — commit cee8383 (forge 103, sdk 37, web 31; ack Yul first leaf 452,096 gas; runtime 12,411 B). Implementer fixed a brief bug (inline leafSig consumed vm.prank/expectRevert) — accepted. Review dispatched (sonnet).
- Task 5: complete — commit cee8383 (review opus: ✅ spec, Approved, 0 Critical/Important; adversarial walk-through (a)–(h) clean). Minors → Task 8 Step 3d (discriminating rollover-reset test via vm.load, seq==128 / old-epoch leaf / equal-amount tests, ArbSys staticcall, POSEIDON_STYLUS known-answer check, .env.example comment). Ruling: keep `delete filledSubtrees` (explicit reset; gas cost once per epoch is acceptable). Hand-off to Task 7 confirmed already in plan: client verifies leaf == leafHash(receipt) and A == prev + due before ack; provider checks budget before signing.
- Task 6: dispatched (implementer sonnet), brief task-6-brief.md, report task-6-report.md.
- Task 6: implementer DONE — commit 2e4f1ab (cargo test 7/7; cargo stylus check OK, WASM 18,418 B compressed; ABI selectors match Yul twin). Review dispatched (sonnet).
- Task 6: complete — commit 2e4f1ab (review sonnet: ✅ spec, Approved; constants/round loop/insert semantics/selectors independently verified; 7/7 native). Minors parked: revert precedence differs from Yul when multiple inputs invalid (not observable by AegisChannel); test helper uses macro-private `__stylus_host` (Cargo.lock pinned); cfg warnings from stylus-sdk macros.
- Task 7: dispatched (implementer sonnet), brief task-7-brief.md, report task-7-report.md.
- Task 7: implementer DONE — commit ab06d04 (sdk 41/41 ×2, web 31, forge 103; anchored ack Yul ≈307k/unit steady, 449k cold). Deviations accepted (exit fallback order, dispute guard scoped, hashDomain generic). Review dispatched (opus).
- Task 7: review (opus) → Needs fixes: CRITICAL client trusted the 402 `anchored` flag (a lying co-signed provider could skip the exit-ticket check → deposit lock; plan-mandated); IMPORTANT dispute()/exitUnilateral() fail when the provider called startClose first (penalty path unreachable). Ruling: mode derived from factory.POSEIDON() on-chain (402 flag cross-checked), dispute/exit tolerate CLOSING + seq==tree.size pre-check, provider state gate + startup mode assertion, reply shape guard; tests: evil anchored flag (port 4029), provider-first startClose then dispute. Fix round 1 → resumed implementer a6db57f. Parked: cached-leaf reconciliation after a lost ack tx (liveness only).
- Task 7: fix round 1 → commit bc43298 (sdk 43/43 ×2, web 31). Scoped re-review dispatched (sonnet).
- Task 7: complete — commits ab06d04 + bc43298 (re-review: all findings addressed). Robustness notes → Task 8 Step 4b (modeCheck cache reset on rejection; exit error text). Parked: dispute() on an undeployed channel gives a low-level viem error.
- Task 8: dispatched (implementer sonnet), brief regenerated after plan patch.
- Task 9 (controller) partial: Stylus AegisPoseidon deployed+activated on 46630: opt-z build 0xd2b670748bf445543c94c010def03abcc04c37a7 (insertPath est 231,217; hash2 91,259), opt-3 build 0x65b059d9ff43720ff50bd5a7daa1a5b869b4f038 (insertPath 175,905; hash2 83,172; 20.1 KB). Yul twin on testnet: PoseidonT3 lib 0xd52e29197D7Fc27FB79240097B92a169408Ad3d1, PoseidonPathYul 0x804318aE7b0cFCE9e1995A84B7833d95B2713766 (insertPath 252,271; hash2 62,138). Outputs identical (hash2(1,2), roots[0]). Finding: Poseidon v1 via generic ark-ff only reaches 1.43× total (≈1.50× exec) vs Yul — the 2.33× D2 number came from OZ Poseidon2 (circuit-incompatible). Ruling: Task 6b — port circomlibjs poseidon_opt (sparse-matrix partial rounds) to push Stylus ≥1.5×; if it fails, keep Stylus (parity, honest docs) — not blocking.
- Task 6b: implementer DONE — commit fb7ec03 (poseidon_opt port, 9/9 tests, 21.7 KB). Controller deployed on 46630: 0x1027cf7dc26152012ed9ef949aa1432bf1c7ef34 — insertPath est 144,076 (Yul 252,271 → 1.75× total, ≈1.90× exec-only), hash2 82,593; outputs identical. D2 threshold (≥1.5×) met with circuit-compatible Poseidon v1. POSEIDON_STYLUS for DeployTestnet = 0x1027cf7dc26152012ed9ef949aa1432bf1c7ef34. Review of 6b pending (sonnet, after Task 8 report).
- Task 8: implementer DONE — commits 122672f + c90b16b (forge 110, sdk 48, web 31). Review dispatched (sonnet). Task 6b review dispatched (sonnet) in parallel (read-only).
- Task 8: complete — commits 122672f + c90b16b (review sonnet: all 10 steps ✅, Approved). Minors parked (duplicate seq-128 test; SIGINT registration order; DeployTestnet path only exercised on the real chain — Task 9).
- Task 6b: complete — commit fb7ec03 (review sonnet: Approved; JS↔Rust index arithmetic, constants, pairwise oracle, sum_of_products exactness all independently verified). Minors parked (full_round length assert, README "no_std" wording).
- Task 9 (controller): DeployTestnet failed in simulation on the Stylus known-answer check (OpcodeNotFound — Foundry cannot execute WASM) → fixed via vm.rpc eth_call helper (commit above). Broadcast OK on 46630 with POSEIDON_STYLUS=0x1027cf7D…ef34, 7/7 verified: usdg 0x7455E600be30C511175B410453E8a23dF52E94EF, verifier 0x7B8ad2d9e848e4Ac62f6Df30283D50Df36D56273, factory 0x596E9f218a0e73Cb8a22F5cDcb93FD681F24dAfF, factoryProd 0xf8e93aE5790a3484874963ADE7397Bc5006dFE5a, factoryAnchored 0x1fB7d8E1445802508c1003aE4543467a3f7c6147, router 0xe4A87335A54d50bf5E2afd830dc917b33b697689, escrow 0xC4b08e8Fd02ef1549F129A509A04a8e6319fAb9C, deployBlock 121982356. v0 JSON kept as deployments/testnet-46630.v0.json (gitignored).
- Task 9 (controller): integration suite on 46630 → 13 passed, 4 skipped (Anvil-key-only), 973 s. Gas on testnet: co-signed dispute fund 57,905 / submitCheckpoint 118,113 / claimPenalty 308,256 / settle 102,427; anchored (Stylus 0x1027cf7D…) ack first 357,028 then ≈211–217k steady (Yul on Anvil ≈303–309k), startClose 65,484. Scenarios 11 (rollover), 13 (router→treasury), 12 (anchored ×4) all pass on the public chain.
- Task 9 docs: commit 9aa2928 (README v1 addresses/liveness/P1 section, benchmarks §5, spec §8.7/§19 V18b/§20, TOOLCHAIN, .env.example). Final whole-branch review dispatched (opus).
- Final whole-branch review (opus): With fixes — I1 rollover hostage window (pre-signed next-epoch exit ticket), I2 HOOK_GAS margin/NatSpec; minors. Ruling: single fix dispatch (final-fix-brief.md); contracts source bumps HOOK_GAS to 300k but testnet v1 stays deployed at 150k (documented) — v2 redeploy deferred to the end of Plan 2c with the Slither fixes. Parked: per-session mutex included as M6; multicall readChannel; T15 keepalive → P2.
- Final fix → commits cc9fc92 + 291b6c5 (forge 111, sdk 50, web 31). Scoped re-review dispatched (sonnet).
- Final re-review: all findings addressed (I1, I2, M1–M9). Full suites on branch tip: forge 111, circuits 14, demo 3, stylus 5, sdk 50, web 31 — green. Parked: jobChain map never pruned; /ack not serialised; anchored rollover() untested; T15 keepalive P2; HOOK_GAS 300k not yet on testnet (v2 redeploy at the end of Plan 2c).
